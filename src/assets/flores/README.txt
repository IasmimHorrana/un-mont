Coloque aqui os PNGs finais das flores, nomeados exatamente como o `flor_id` da planilha
(ex.: flor_01_girassol.png, transparente). O app detecta o arquivo automaticamente
(FlorIllustration usa import.meta.glob) e para de usar o placeholder SVG — nenhuma
mudança de código é necessária.
